<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %blue(%d{yyyy-MM-dd HH:mm:ss}) %highlight(%level) %cyan([%thread]) %boldGreen(%logger{15}) %gray(%file:%line) %msg%n
            </pattern>
        </encoder>
    </appender>
    <logger name="com.zoecll" level="info" additivity="false">
        <appender-ref ref="Console"/>
    </logger>
    <root level="info">
        <appender-ref ref="Console"/>
    </root>
</configuration>